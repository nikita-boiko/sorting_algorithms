# Реализация базовых алгоритмов сортировки на Python

Этот репозиторий содержит примеры классических алгоритмов сортировки и слияния списков, написанные на языке Python.

## Содержание

1. [Слияние двух отсортированных массивов](#1-слияние-двух-отсортированных-массивов)
2. [Сортировка вставками (Insertion Sort)](#2-сортировка-вставками-insertion-sort)
3. [Сортировка выбором (Selection Sort)](#3-сортировка-выбором-selection-sort)
4. [Пузырьковая сортировка (Bubble Sort)](#4-пузырьковая-сортировка-bubble-sort)

---

### 1. Слияние двух отсортированных массивов
Алгоритм объединяет два уже отсортированных списка (`a` и `b`) в один общий отсортированный список `c`. Этот метод является ключевым этапом в алгоритме **Merge Sort**.
* **Сложность:** $O(N + M)$

### 2. Сортировка вставками (Insertion Sort)
Алгоритм перебирает элементы и вставляет каждый на своё место в уже отсортированной части массива, сдвигая остальные элементы.
* **Сложность:** $O(N^2)$
* **Особенность:** Эффективен на небольших или частично отсортированных данных.

### 3. Сортировка выбором (Selection Sort)
На каждом шаге алгоритм ищет минимальный элемент в неотсортированной части массива и меняет его местами с первым элементом этой части.
* **Сложность:** $O(N^2)$
* **Особенность:** Количество обменов (swap) минимально.

### 4. Пузырьковая сортировка (Bubble Sort)
Самый простой алгоритм: он многократно проходит по массиву, сравнивая соседние элементы и меняя их местами, если они стоят в неправильном порядке. Большие значения постепенно «всплывают» в конец.
* **Сложность:** $O(N^2)$

## Как запустить

Убедитесь, что у вас установлен Python 3. Скопируйте код в файл (например, `sorting.py`) и запустите его:

```bash
python sorting.py
